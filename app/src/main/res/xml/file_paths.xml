<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Photos attached to transcripts/notes live here. -->
    <files-path name="attachments" path="attachments/" />
    <!-- Recorded and imported audio, for "Share audio". -->
    <files-path name="recordings" path="recordings/" />
    <files-path name="imports" path="imports/" />
    <!-- Staged share/export files. -->
    <cache-path name="cache" path="." />
</paths>
